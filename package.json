{
  "name": "flask-tailwind-alpine-starter",
  "version": "1.0.0",
  "description": "starting point for a flask project using alpine.js and tailwind.css",
  "main": "index.js",
  "scripts": {
    "about:test": "",
    "test": "echo \"Error: no test specified\" && exit 1",
    "about:watch": "",
    "watch": "./node_modules/.bin/postcss ./static/src/main.css --dir ./static/src/dist --watch",
    "about:build": "echo I am not even sure if this line is necessay anymore, since you should just setup watch",
    "build": "./node_modules/.bin/postcss ./static/src/main.css -o ./static/dist/main.css",
    "about:dev": "echo start flask server under watch mode",
    "dev": "FLASK_APP=app.py FLASK_ENV=development flask run"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.3.3",
    "postcss": "^8.3.6",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^2.2.9"
  }
}
